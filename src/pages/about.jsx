import React from "react";
import Layout from "../components/Layout";

const About = ({ data }) => {
  console.log(data);
  return (
    <>
      <Layout>
        <p>About</p>
      </Layout>
    </>
  );
};

export default About;
