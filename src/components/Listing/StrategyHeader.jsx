import React, { Component } from "react";
import styled from "@emotion/styled";
import { StaticQuery, graphql } from "gatsby";

const StrategyHeader = () => {
  return <h3>Strategy is</h3>;
};

export default StrategyHeader;
